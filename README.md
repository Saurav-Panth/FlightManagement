# FlightManagement
FlightManagement API is a Spring Boot–based RESTful application that manages flights, bookings, passengers, and payments. It is designed to handle end-to-end flight booking operations with proper exception handling and clean API structure.
